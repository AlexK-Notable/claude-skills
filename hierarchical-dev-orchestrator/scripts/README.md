# Scripts Directory

No scripts needed for this skill. The orchestration happens through Claude's native tools:
- `view` for file reading
- `str_replace` for editing
- `bash_tool` for test execution
- `create_file` for new files

This directory exists to maintain standard skill structure.
