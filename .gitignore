# Runtime state generated by hypr-doctor itself (lives in ~/.cache, not here,
# but keeping this file present so accidental future stashes here are ignored)
*.cache/
last-run

# Editor cruft
*.swp
.DS_Store

# Build artifacts (none today, but defensive)
*.so
build/
